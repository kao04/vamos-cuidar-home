# .htaccess
# Ativar o módulo de reescrita
RewriteEngine On

# Base URL da aplicação (a subpasta)
RewriteBase /vamoscuidar/

# Se a requisição NÃO for para um arquivo existente (-f)
RewriteCond %{REQUEST_FILENAME} !-f

# E a requisição NÃO for para um diretório existente (-d)
RewriteCond %{REQUEST_FILENAME} !-d

# Redirecionar tudo para o index.html, deixando o React Router cuidar do resto
RewriteRule ^ index.html [L]